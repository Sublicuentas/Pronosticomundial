# Guarda este archivo en:  .github/workflows/update-elo.yml
# Actualiza ratings.json (Elo+forma), results.json (marcadores) y wc_fixtures.json
# (partidos resueltos de TODAS las rondas) sin que toques nada.

name: Actualizar Elo

on:
  schedule:
    - cron: '0 */4 * * *'   # cada 4 horas
  workflow_dispatch:          # boton para correrlo a mano

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Bajar Elo, forma, resultados y fixtures
        run: node fetch-elo.js
        env:
          FD_KEY: ${{ secrets.FD_KEY }}   # llave gratis de football-data.org

      - name: Commit si cambio
        run: |
          git config user.name "elo-bot"
          git config user.email "actions@github.com"
          git add ratings.json
          [ -f results.json ] && git add results.json
          [ -f wc_fixtures.json ] && git add wc_fixtures.json
          if [ -n "$(git status --porcelain --cached)" ]; then
            git commit -m "Datos actualizados"
            git push
          else
            echo "Sin cambios."
          fi
